% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{push_flagged_svs}
\alias{push_flagged_svs}
\title{A phylosql Function}
\usage{
push_flagged_svs(
  source = "flagged_sv",
  route_col = NULL,
  mapping = NULL,
  targets = NULL,
  con = NULL
)
}
\arguments{
\item{source}{the name of the flagged source table. Defaults to "flagged_sv".}

\item{route_col}{name of the column in \code{source} that identifies the target
database for each row (e.g. "target_database", "Kingdom"). The values in
this column are either table names directly, or keys to look up in \code{mapping}.}

\item{mapping}{an optional named character vector translating values found in
\code{route_col} to target table names, e.g.
\code{c(Bacteria = "bacteria_sv", Eukaryota = "eukaryota_sv")}. If NULL, the
values in \code{route_col} are used as table names verbatim.}

\item{targets}{optional character vector restricting which target tables to
push to. If NULL, all targets present in the routed data are pushed.}

\item{con}{connection}
}
\description{
Push rows from a \code{flagged_sv} table out to their respective SV databases.
}
\details{
Reads the flagged_sv table, groups rows by their target database (determined
either from a routing column in flagged_sv or from a user-supplied mapping),
and appends only the rows whose MetagenNumber+SV combination is not already
present in the target. The flagged_sv table itself is not modified.
}
